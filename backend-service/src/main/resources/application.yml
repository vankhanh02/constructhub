server:
  port: 8080
  address: 0.0.0.0

spring:
  application:
    name: backend-service
  profiles:
    active: '@spring.profiles.active@'

management:
  endpoints:
    web:
      exposure:
        include: '*'

logging:
  level:
    root: INFO
    web: INFO